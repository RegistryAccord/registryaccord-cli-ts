# RegistryAccord CLI environment variables
# Copy this file to .env and adjust values as needed.

# Base URLs for services (used by src/lib/config.ts)
RA_IDENTITY_URL=http://localhost:8080
RA_CDV_URL=http://localhost:8081

# HTTP client timeout in milliseconds (used by src/lib/http.ts)
RA_HTTP_TIMEOUT_MS=5000
